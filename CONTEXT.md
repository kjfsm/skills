# kjfsm Skills

Claude Code が読み込むエージェントスキル(スラッシュコマンドと振る舞い)の集合。スキルはバケットに整理され、`/setup-skills` が出力するリポジトリごとの設定によって利用される。

## 用語

**イシュートラッカー**:
リポジトリのイシューを保持するツール — GitHub Issues、Linear、ローカルの `.scratch/` マークダウン規約など。`to-tickets`、`to-spec`、`triage` などのスキルがここから読み書きする。
_避ける表現_: backlog manager, backlog backend, issue host

**イシュー**:
**イシュートラッカー** の中で追跡される単一の作業単位 — バグ、タスク、スペック、または `to-tickets` が生成するスライス。
_避ける表現_: ticket(外部システムが「ticket」と呼んでいることを引用する場合、または後述の **意思決定チケット** の場合のみ使用する)

**意思決定チケット**:
`wayfinder` の単位。`wayfinder:map` の子となる **イシュー** で、解決すべきものがビルドを実行するスライスではなく _問い_ であるもの。**決定**という修飾語が、実装チケットと区別する要である。`wayfinder` がこの用語を導入した後は「ticket」を使う。

**検証ゲート**:
変更が動くことを確かめるために走らせる、合否が二値で出る1つのコマンド(型チェック、lint、テスト、ビルド、そして変更した経路を実際に駆動する **観測**)。並びと順序は `/setup-skills` が `docs/agents/verification.md` に記録し、`/verification-loop` がそれを走らせる。
_避ける表現_: 自動チェック, validation step

**カスタムチェック**:
汎用のリンターが見逃す、そのリポジトリ固有の決定的なルール。`docs/agents/verification.md` に書かれ、**検証ゲート** の1つとして差分に適用される。判断が要るものはカスタムチェックではなく、リポジトリのコーディング標準に属する(`/two-axis-review` が読む)。

**トリアージロール**:
トリアージ中に **イシュー** に付与される正規のステートマシンラベル(例: `needs-triage`、`ready-for-afk`)。各ロールは `docs/agents/triage-labels.md` を通じて **イシュートラッカー** 内の実際のラベル文字列に対応付けられる。

## 関係性

- **イシュートラッカー** は多数の **イシュー** を保持する
- **イシュー** は常に1つの **トリアージロール** を持つ
- **意思決定チケット** は **イシュー** である(`wayfinder:map` の子)
- **検証ゲート** は順序を持つ列をなし、その1回の通し実行が **クリーンラン** である
- **カスタムチェック** は **検証ゲート** の一種である

## 注意すべき曖昧な用語

- "backlog" は以前、イシューを保持する _ツール_ と、その中の _作業の集まり_ の両方を指す言葉として使われていた — 解決済み: ツールを指すのは **イシュートラッカー** であり、"backlog" はもうドメイン用語として使わない。
- "backlog backend" / "backlog manager" — 解決済み: **イシュートラッカー** に統合された。
- "自動チェック" は以前、走らせるコマンドの列を指す言葉として使われていた — 解決済み: **検証ゲート** が唯一の呼び名であり、"自動チェック" はもうドメイン用語として使わない。
